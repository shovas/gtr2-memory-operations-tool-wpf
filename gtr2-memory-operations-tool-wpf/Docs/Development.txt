SHO GTR2 Memory Operations Tool

DEVELOPMENT

Reminders

- A DLL in the future will make some things much more automatic. Won't have to startup the app every time you start GTR2. So a lot of the annoyances aren't permanent.

- Session > Timing > Sector has Weight Penalty, Sector 1 Laptime, Sector 2 Laptime, Laptime, Split Time
  - **Split Time**

Todo

- Gtr2SharMemOps could watch shared memory itself and fire events that callers can listen for
  - eg. Session start/end, driver completes new lap, driver new best laptime, etc.
  - This would make things easier by building up small useful pieces to build something bigger

- Consider: Converting Gtr2GridVehicleSlot to a struct should be much faster.
  - If I can get away without the fancy class logic then it'll be a win.

- Could Gtr2MemOps.ReadGtr2GridData() just read all memory right through instead of in a loop which is probably slower?
  - The solution is probably to read it like the shared memory implementation where it reads whole structs right in

- Session Restart = Game Phase Change to 5=GreenFlag twice?

- Update weight calculations whenever any driver posts a new laptime, not just the player, to have finer grained BOP
  ^ This will be needed for when the driver is still behind but stopped driving eg. AI does this in practice sometimes

- Automatic AI Improvements
  - Todo: +0.03s/kg needs tuning, it's very far off the mark right now...[days later]...maybe not now that the algo is working better. Let's see.
  - Todo: Update weight calculations whenever any driver posts a new laptime, not just the player, to have finer grained BOP
    ^ This will be needed for when the driver is still behind but stopped driving eg. AI does this in practice sometimes

Doing

- Implement Automatic AI tab
    - Bug: Sometimes Automatic AI tab Last Lap column never updates if started before drivers have laptimes in even after they start putting in laptimes. A reset will usually start displaying the new laptimes correctly. But it should work from the start.
    - Todo: List of Drivers and their current AI adjustment values
      - Todo: Run an AI adjustment calculation every refresh and record adjustment with each driver
        - Note: Reminder this AI adjustment is a weight penalty adjustment: +0.03s for every 1kg added.
        - Todo: Activate: Start the automatic adjustment algorithm
        - Todo: Deactivate: Stop the automatic adjustment algorithm
        - Todo: Display current AI adjustment for each driver (at least a holder for now)
        - Todo: AddLogItem() should log to file for easier review.
        - Bug: Updating program memory grid vehicle slots parameters eg. WeightPenalty persists across session restarts (ESC > Restart)
          - Zero out manually on session restart?
          - Scratch: Workaround: Start new session from main menu
            ^ Starting a new race weekend from the main menu still had weight penalties applied.
          - I guess I need to zero out the grid's weight penalties, maybe I can watch for an event like green flag
        - Done: Weight Penalty Algorithm
          - This is an ultra rough first pass but it is calculation weight penalties and applying them.
          - Once drivers have at least N laptimes recorded, compare to player and calculate either a player weight penalty (too fast vs ai) or an ai driver weight penalty (too fast vs player)
            - Player and comparison AI must have at least N laptimes recorded for this to work
            - Comparison AI is P2 AI when Player is P1 and P1 when player is any other position
            - Start with 3 laptimes to make this a little easier. Make it configurable.
            - Ended up just waiting for all players to have a best laptime recorded.
          - Fixed: Sometimes when loading into a track the grid only loads in partially eg. some names are missing or all of them are missing.
            - Not getting a full complete read until fully into the session?
            - Doesn't even fix the blank cells in AAI tab data grid when refreshing program/shared memory.
            - I need an event I can wait for: Session change, Game phase changed, etc., some combination. Green flag always fires.
            - Done: Now waiting for Shared Memory mSessionStarted until refreshing drivers list.
              - mSessionStarted guarantees any session has started.,
              - mSessionStarted is not Game Phase Green Flag, it enables as soon as you enter the garage, so it's ideal for an event to wait for to read consistent shared memory.
          - Fixed: When player is in first, weight penalty keeps increasing even though player laps since the weight penalty are slower than some AI's best laptimes.
            - Scratch: I'm doubling up on weight penalties, I think, not taking into account what's already there
              ^ No, doesn't seem to be.
            - Done: I'm always comparing against the player's best laptime *before* weight penalties were applied. So the player always looks faster than they now are.
              - So, I need to compare the best laptime since the last weight penalty adjustment.
              - Done: Track laptimes and exclude all laps prior to the lap that received the weight penalty adjustment
                - Done: Review this logic and move on. I think the logic is okay, just need to review.
                - Fixed: BopLap didn't match Total Laps (last completed lap).
                - Scratched: In the "Not adjusting AI driver weight penalties...player not slower than leader" (player still not P1), AI still need to have weight penalty reduced not increased to keep competitive with player.
                  ^ Scratch: This was in the player-not-in-first condition so the player needs to catch up to P1 before we consider reducing AI weight penalty.
               - Fixed: Driver laptimes are sometimes 'dropping' laps (or something) and then BopLap can end up greater than laptimes recorded.
                  - Note: Related: "Error: Aborting weight penalty calculations. Player driver laptimes count (8) less than BopLap (10)."
                  - Note: Laptime Changed event triggers based off shared memory changes. Are we missing laptime changes somehow?
                  - Fixed: Still happening. Saw "P2 AI driver laptimes count (6) less than BopLap(8).
                    ^ Missed updating settings.ini Gtr2SharedMemoryWatcher.WatchSharedMemoryTime from 5 to 2 in the previous fix. This might be why lap tracking was missing some laps.
                  - Fixed: Laptime changed event now based on changing laps not a difference in laptimes which was probably getting missed because (1) small chance two laps can have the same laptime or more likely (2) old vs new snapshot were the same (not old lap vs new lap) and so never triggered.
                    - Update: Actually this was probably the 5sec interval to refresh program memory and shared memory that entire laps were missed under time acceleration because laps went by so fast.
                - Fixed: Race condition with LoadDrivers recreating AaiDrivers.
                  ^ Now reusing existing AaiDrivers instead of recreating them each refresh.
        - Done: Need to collect laptimes for each driver so I can calculate an average laptime for them eg. last N laps
         - Done: Listen for new laptimes for each driver and track laptimes in an array myself for later Automatic AI calculations
          - Done: Gtr2SharMemOps or a new class should fire events for what I need to watch for eg. when new laptimes are recorded
      - Done: Deactivate: Stop drivers/shared memory refresh timers
      - Done: Activate: Start drivers/shared memory refresh timers
      - Done: Reset: Clear Drivers list
      - Done: Refresh button: Re-read memory and update UI list of drivers from grid vehicle slots
      - Done: Driver names are the current driver in the car so you can't always match up with GTR2's Timing screen. 
        - Done: Select first mDriverName (first Gtr2VehicleScoring) in shared memory and compare to NameFull_One and NameFull_Two from Program Memory to determine who is currently in the car.
        - Will this be a problem for Automatic AI? I don't think so since the adjustment will be against the car not the driver.
     - Done: Refresh Automatic AI tab on an interval
      - Done: Xaml interface 
    - Done: Reading grid memory is too slow for the usage that I need.
      - Done: Reading class with minimal fields (driver name, last laptime) is fast enough (100ms for 10 drivers)
        - I'm sure there's other ways to optimize this because I think the time is going into allocating memory for each field and the class itself.
      - Done: Create a class like Gtr2GridVehicleSlot that contains only fields needed for Automatic AI tab
        - Eg. Fields: Name and Last Laptime for now
      - Done: Investigate if slowness is searching through memory to find GTR2 Grid Address or reading fields after finding the grid
        - Problem is reading memory for each field, not searching through memory to find GTR2 Grid address.
        - Reading field by field takes 50sec
        - Reading the whole Slot's bytes at once (22624) and parsing fields out of that takes 30sec
        - So, solution is probably just reading the fields we need
    - Done: ListView logging adjustments as they happen
      - Done: Xaml interface 
  - Done: Add new tab for Automatic AI adjustments
    - Done: Button bar: Refresh, Reset, Activate, Deactivate
      - Done: Reset button
      - Done: Activate button
      - Done: Deactivate button
      - Done: Refresh button
      - Done: Xaml interface 

Done

- Window now remembers its size and position on screen between sessions

- Separate logging level for log file

- Make this a setting in SharedMemoryView.xaml.xs LoadItems: int batchLimit = 50;

- Fixed: If not in a race session and you refresh Program Memory, nothing happens and Refresh button stays inactive forever

- Fixed: Shared Memory tab displays corrupted data after byte[] driver name for mPlayerName, mDriverName, etc.
  - Yep: Null terminator?

- Implement Program Memory tab memory reading and writing
  - This tab will be for reading/writing values in the GTR2 process memory that aren't in the shared memory, such as player name, car setup values, etc.
  - Done: Allow editing ListView values and writing them back to memory (use Settings tab for reference)
    - Done: Allow editing ints
    - Done: Allow editing floats
    - Done: Allow editing strings
    - Scratch (no bools in Grid Slot data yet): Allow editing bools
    - This will be a proof of concept protoype bit of logic for the Dynamic AI Adjustment Algorithm that I want to implement later. I want to be able to edit values in memory and see the effect in game before I start building out the algorithm.
  - Done: I want to allow double-clicking the ListView to open another Window as a modal popup that show the row's value, allows editing, and allows saving, which writes the new value to memory, or canceling.
    - https://claude.ai/share/2b9844a1-117a-4e92-9901-259f885f320c
  - Done: Sync up Addresses/Offsets between Cheat Engine and Gtr2MemOpsTool so that I can leverage both at the same time. Should help accelerate memory work.
    - Use hex instead of uints in UI? To match up with CE, yes.
  - Done: Add Buttons row like Shared Memory tab
  - Done: Add ListView like Shared Memory tab
  - Done: Add memory reading logic: I already have a lot of this in Gtr2MemOps
  - Fixed: Current problem is I'm off by 4 bytes in my Gtr2GridVehicleSlots memory items.

- 32bit offsets vs 64bit addresses issue
  - GTR2.exe is a 32bit process, so all memory addresses are 32bit. 64bit nints can handle addresses but when I calculate nint offsets based on them the offsets are 64bit/8-byte and calculations can end up wrong.
  - I think I need to make sure all of my offset calculations are based on 32bit integers and then only convert to nints when I actually read/write memory.

- Log should simulaneously write to a log file

- Add settings ini logic, use object to hold settings in memory, add a settings tab to the UI to edit them, and save them back to the ini file
  - Log Level setting
  - Set IniData as App.Config to use everywhere

- Deal with non-error/non-warning VS messages. Good learning experience.

- Fix Log view filtering to handle new ListView implementation
  - See ChatGPT about binding a view instead of an items list and then you filter the view instead of the items. 

- Moved Log tab TextBox to ListView for performance
  - Problem: Log tab is freezing after Refreshing on Shared Memory tab
    - 17000 memory fields, 21000 log lines generated
    - Was able to optimize it with a few techniques but pretty much confirmed TextBox is doing too much to handle the text as a single thing.
      ^ Solution: Move to ListView.
        - ListView is able to handle thousands of log lines because of its narrow view window technique
  - Copy Log button will have to first build a string out of all rows to copy

- Shared Memory tab Index is always -1 now

- Display nested structs in Shared Memory as key/value pairs
  - Chunk GetGtr2SharedMemoryItems() onto background thread so that I can update the UI incrementally

- Move all logging to AsyncBatchLogger

- Decouple app logic from UI update logic that was causing UI responsiveness issues.
  - aka Get UI updates out of non-UI code (ie. non-xaml cs): eg. App.Log calls

- Add Shared Memory tab button bar Filter By Memory Region: Telemetry, Scoring, PitInfo, Extended, HWControl, etc.

- Add Shared Memory tab button bar Refresh button to re-read the shared memory and update the display

- Add Shared Memory tab button bar Filter by Text box by searching on key

- Add button bar to Shared Memory tab: Filter by Text, Filter by Region

- Add LogView buttons: Clear Log, Copy Log, Save Log, Log Level

- Log tab is now the primary tab, shows a welcome message, does some startup checks like check for GTR2 process

- Made MainWindow responsive to window resizing

- Added log levels: Debug, Info, Warning, Error, Exception. Will add log filtering by level later.

- Add Help > Suppory My Work menu item with link to SimWiki support page

- Port CCGEP Monitor's shared memory reading logic to the new WPF app
  - Displayed as new Shared Memory tab
  - Menu item added to test shared memory
  - Running shared memory test populates Shared Memory tab with a key:value table
  - Hardwired to show Extended region data for now

- Show a StatusBar welcome message for a few seconds and then indicate ready

- Port the basic console app logic as a starting point

- Port classes from gtr2-memops9 Windows Console App to the new GUI app

- Initial work with the Log tab and getting it read to display real log messages

- Upgrade to .NET 8

- Initial prototyping of Windows GUI app with standard menus and a tabbed interface